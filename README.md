# webserver
Local web server project with boot.dev.

## What it does
This is a Typescript/Node project to practice creating web servers and using Postgres to make a microblogging site with various endpoints and query options.  

## How to install and run it
This project uses expressjs to handle endpoints, Postgres for the databases of users, texts, and authentication/authorization, and Drizzle for SQL queries and migrations.